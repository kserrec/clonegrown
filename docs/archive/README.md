# Historical evidence

These two files record the August 29, 2026 real-repository experiment against
pinned Git and curl commits. They retain the original source-tree hash,
environment, fixture identities, results, and limitations. They are evidence
for that older tree only, not qualification of the current package.

See [the dated report](REAL_REPOSITORY_QUALIFICATION.md) and its
[raw result](REAL_REPOSITORY_QUALIFICATION.json). Replaying today's retained
`tests/campaign/real_repository_qualification.py` produces a new result, not a
byte-for-byte reconstruction of that historical run. Run it from the repository
root as the report describes.

Current product behavior is documented in the root README and ARCHITECTURE.
